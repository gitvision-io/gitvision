query GetAllReposOfUserV2 {
  viewer {
    login
    repositories(first: 100) {
      pageInfo {
        hasNextPage
        endCursor
      }
      edges {
        cursor
        node {
          id
          name
          isInOrganization
        }
      }
    }
  }
}
