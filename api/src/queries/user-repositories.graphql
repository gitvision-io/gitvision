query GetAllRepositoriesForUser {
  viewer {
    repositories(first: 20) {
      edges {
        node {
          refs(refPrefix: "refs/heads/", first: 100) {
            nodes {
              name
            }
          }
          id
          name
        }
      }
    }
  }
}
