query GetAllRepositoriesOfOrganization($login: String!) {
  viewer {
    organization(login: $login) {
      repositories(first: 10) {
        edges {
          node {
            name
            id
            databaseId
            visibility
          }
        }
      }
    }
  }
}
